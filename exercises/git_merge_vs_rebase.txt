


- execute two flows with one server and two users.
	One does things with rebase
	the other with merge.
	What is difference in the logs after the entire merge is over?

- do the same as above but create a real conflict.
	Now answer the same question:
	What is difference in the logs after the entire merge is over?
